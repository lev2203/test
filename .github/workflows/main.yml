name: cicdTest


on:
  push:
    branches: 
            - main
jobs:
 my_testing:
    runs-on: ubuntu-latest

    steps:    
      - name: Print Run Test
        run : echo "Hello, Test"
        
        
 my_deploy:
   runs-on: ubuntu-latest
   
   steps:
     - name: Print Run Deploy
       run : echo "Hello, Deploy"
